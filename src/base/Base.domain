package base;

import external.*;
import ddt.lang.*;
import Resources.*;

domain Base {
	variables: {
		public static Dinteger total = new Dinteger(0);
		dtenum State{START,END}
		DTenum state = State.START;
	}
	
	events: {
		Add;
		Exit;
	}
	
	resources: {
		public static SyncTool st;
	}
}